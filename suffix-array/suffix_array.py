#TODO
# Given a string S, construct its suffix array (indices of all suffixes sorted lexicographically).
# Implement a function search(pattern) that checks whether pattern exists in S using binary search on the suffix array.